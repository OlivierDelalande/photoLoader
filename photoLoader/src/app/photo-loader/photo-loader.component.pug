button((click)="sendJSON()") Send JSON

form([formGroup]="loadPictureForm", (ngSubmit)="loadPicture(loadPictureForm.value)", [class.submitted]="submitted", [class.ng-invalid]="errorServer", novalidate)
  input-file(formControlName="file")
  button(type="submit") Send Picture
